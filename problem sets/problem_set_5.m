clc; clear; close all;

%% Load data
% load the file "campus19.mat" into your MATLAB workspace


%% Per-mouse means
% Use a for loop to calculate mean PCA score of each mouse across all 
% timepoints. Then use the `disp` function to report the mouse number and 
% its mean PCA score.

% Use something like this:
% disp("Mouse " + nan + " experimental group " + nan + " mean weight: " + nan + " grams.")


%% Per-timepoint means
% Use a for loop to calculate mean weight for all mice at each timepoint.
% Display the information in a similar way to the previous problem.



%% Use a for loop to plot the scores of all of the mice over time.
% This is the same as in the previous assignment, but this time use a for loop.
% The for loop should iterate over the mice, plotting one line at a time.
% remember to `hold on`!



%% Plot the weights of all of the mice over time, but color the lines based on the experimental group of each mouse.
% You will want to use an 'if' statement.

